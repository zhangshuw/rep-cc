kind = "web"
previewPath = "/"
title = "Free Claude & GPT API Portal"
version = "1.0.0"
id = "artifacts/api-portal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24927

[services.development]
run = "pnpm --filter @workspace/api-portal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/api-portal", "run", "build" ]
publicDir = "artifacts/api-portal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24927"
BASE_PATH = "/"
